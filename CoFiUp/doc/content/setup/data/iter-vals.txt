this::
    date = 2023-01-???


=======================
???
=======================

????

ok ici car ¨numver sans release, si release à géré on devra passer via traotement python car trop évolué ! par contre dans config, on peut indiquer des fonction maison pour ajourer des filters comme avec jinjang !!!!

  allversions:
    - dt_version
    - ...

équivuat à

  allversions: [dt_version, ...]

si besoin de garder dates au lieu de _ pour ifnore et dans ce cas le reste continue tout de même à fonctionner !!!

      - history:
        - date @ dt_date: version @ dt_version ...


yaml::
    ---
    title = Le fichier path::``VERSIONS.yaml``
    path  = ?/examples/autoversion/VERSIONS.yaml
    ---

idéé de récupérrer dernier nuémro de version, mais on idnique que ceci n'est qu'un travail prépa car on verra comment obtenir efficvement le bon remplace plus tard !

toml::
    ---
    title = Le fichier path::``pyproject.toml`` de ¨cofiup
    path  = ?/examples/about2pyproj/pyproject.toml
    ---

cofiup::
    ---
    title = Le fichier path::``config.cfp.yaml``
    path  = ?/examples/autoversion/config.cfp.yaml
    ---
