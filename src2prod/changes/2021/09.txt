==
12
==

**New sub minor version version::``0.0.8-beta``:** the method ``BaseProj.reset`` has an optional argument ``kind`` to indicate the kind of method used to build a project (this is used by ¨texfactory).


==
11
==

**New sub minor version version::``0.0.7-beta``:** the method ``Project.update`` has two new arguments ``opensession`` and ``closesession``.
