project:
  acronym: Speaking, Problems
  desc   : A module to write programs which have to be verbose about
           a specific on files and directories, and that have to emit
           informations, warnings and errors.
  author : Christophe BAL
  licence: gnu 3
  repo   : https://github.com/bc-tools/for-dev/tree/main/spkpb
  require:
      - python3


dist:
  ignore: |
    test_*/
    test_*.*

    tool_*/
    tool_*.*
