this::
    date = 2022-10-18


===================================
Liste complète des ¨params spéciaux
===================================

// AUTO - LIST OF SPECIAL PARAMS - START

    * jinjang::``lang`` est un ¨param optionnel pour indiquer la langue du squelette. Par défaut, la valeur est jinjang::``en_GB``.

    Le type ¨justcode de jinjang::``lang`` est jinjang::``lang``.


    * jinjang::``project_name`` est le nom du projet obtenu en prenant directement le nom du dossier appelant ¨justcode.

    Le type ¨justcode de jinjang::``project_name`` est jinjang::``str``.


    * jinjang::``project_path`` est le chemin absolu du dossier appelant ¨justcode.

    Le type ¨justcode de jinjang::``project_path`` est jinjang::``path``.


    * jinjang::``today`` est la date, sans heure, lors de l'appel de ¨justcode (ce paramètre devient très pratique en étant combiné avec certains des filtres de la série jinjang::``date_...``).

    Le type ¨justcode de jinjang::``today`` est jinjang::``date``.

// AUTO - LIST OF SPECIAL PARAMS - END
